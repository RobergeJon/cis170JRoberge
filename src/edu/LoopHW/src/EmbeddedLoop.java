public class EmbeddedLoop {
	// Program suppose to multiple one variable(determine by for loop) by another variable(determined by an For loop
	//with the answer using a while loop
	//couldn't get to work
	//loops are hard for me to wrap my mind around
  public void Number() {
    int y;
    for (int x=0; x< 10; x++) {
      result = 1;
      y = x;
    }

  }
    public static void main (String [] args) {
        int e;
        int result;

        for (int i=0; i< 10; i++) {
            result = 1;
            e = i;
            while (e > 0) {
                result *= y;
                e--;
            }
            System.out.println( y "to the " + i + " power is " + result);   
        }
    }
}